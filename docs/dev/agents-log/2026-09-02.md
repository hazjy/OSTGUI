# AGENTS-LOG 2026-09-02

> 智能体：`dsh`。
> 注：本日为"文档修正 + 480 联机调研/实测定论"批次。ZTool 内核（v3 邀请修复等）按约定不纳入台账，仅记录 OSTGUI 侧调研结论与决策。

## 变更记录

### 2026-09-02

| 时间 | 类型 | 变更内容 | 涉及 | commit |
|---|---|---|---|---|
| 07:30 | docs | Sudama 缓存策略错误表述修正——README.md 移除「24h TTL」改为「缓存存在即用、不自动过期」并注明手动刷新/导入；DEV-NOTES §11 服务索引措辞修正（"不过期自动刷新"语义矛盾 → "存在即用不自动过期"）；删除 ManifestDownloadService.cs 悬空孤儿注释（描述已移除方法的过时文档注释，含"24h 缓存"）。代码实态（SudamaKeyCache 缓存存在即用、仅供手动刷新/导入更新）此前已与文档多处不符，本次收敛 | README.md, docs/dev/DEV-NOTES.md, main/Services/ManifestDownloadService.cs | 未提交 |
| 07:40 | docs | 480 联机上游外部调研（本机 shell 出网被墙，经 exa/bing 索引检索）：上游 onlinefix 仍在活跃修补——Issue #158（拥有游戏时 onlinefix 路径被完全跳过，2026-07 仍 open）、PR #146（open，支持拥有游戏排队 + P2P 修复）、PR #97 / Discussion #98（EOS 匹配扩展，明确"480 骗术够不到 EOS 流量"，未合并）、PR #144（未拥有 appid 的 persona 广播被 Steam 服务端丢弃）、提交 df3d22a（P2P 游戏修复）、v1.4.7/1.4.8 重心在 Denuvo/manifest 而非 onlinefix；同类 480 方案亦有翻车实录（polden.gg R.E.P.O 联机失败帖） | 外部资料，无代码 | — |
| 07:47 | docs | 480 联机实测定论（用户以**原版内核**实测）：① 僵尸危机：不朽 = BOXHEAD:Immortal(2795640) 能弹出"邀请好友"选项——大厅/lobby 邀请型链路在原版 480 会话下**可用**（阳性对照；虚拟机公网 IP 未验证连通性，不影响 UI 结论）；② PEAK(3527290) 原版/改版均不通——官方确认"纯 Steam 好友邀请"（无 lobby link/code，邀请入口仅在主菜单，社区公认脆弱、正版也频繁翻车），且实测只有 7005 旧式邀请佐证其**不走 LobbyInvite 回调**，v3 补丁对其无效；③ 定性：「内核有一定联机能力但不多」——大厅型可用、无大厅纯好友邀请型死路；④ 决策：ZTool v3 邀请修复**投入暂缓**，PEAK 类不再推进 | 无代码（结论拟沉淀 DEV-NOTES §14，另行处理） | — |
| 08:18 | fix | 联机页游戏名查询修复——用户实测"搜索入库能取到名、联机页查不出"，查证发现取名正源是 `store.steampowered.com` 官方接口（SteamSearchProvider 全源均为官方商店系，搜索/入库即经此取到名并写缓存），联机页此前却取 `api.steamcmd.net`（steamcmd 仅入库取 depots/gid 用）→ 代码问题实锤。修复：`OnlineViewModel.LoadGameNameAsync` 改走与搜索页同源的 `GameSearchService.GetGameNameAsync`（缓存优先→官方 Store API→写缓存），steamcmd 轻量取名 `GetGameNameOnlineAsync`（由原私有 GetNameFromSteamCmdAsync 公开，无 depots gate）仅作兜底；另修 Store 回退路径不解析 name、gid 用非容错 GetProperty 的隐患。构建验证通过，实测"秒出" | main/ViewModels/OnlineViewModel.cs, main/ViewModels/MainViewModel.cs, main/Services/SteamGameInfoService.cs | 未提交 |
| 08:42 | refactor | 清理 Denuvo 页无 UI 触达的方案管理遗留死代码——页面早已重写为"正版授权说明（静态）+ 导入/导出"两面板，但 ViewModel 的方案/列表/编辑/文本导入系列（12 个 RelayCommand、约 20 个属性）、`TicketService` 的 profile 持久化（Load/Save/ApplyProfile/Read/DeleteTicketFromRegistry/ParseTicketFromHex/ParseTicketsFromText/ticket_profiles.json）、`TicketProfile` 模型、`TicketEntry` 显示属性（IsComplete/StatusDisplay/Validity*）全部无 UI 触达。删除前经全仓 grep 复核无隐藏调用方：DenuvoViewModel 536→232 行、TicketService 291→49 行；同步 MainViewModel 构造（删 SteamTicketExtractor 参数与 TicketService 公共属性）与 App DI（SteamTicketExtractor 注册移除；--extract-ticket 子进程模式为直接 new，不受影响）。构建验证通过，导入/导出链路零行为变化 | main/ViewModels/DenuvoViewModel.cs, main/Services/TicketService.cs, main/Models/TicketEntry.cs, main/ViewModels/MainViewModel.cs, main/App.xaml.cs | 未提交 |
| 09:18 | feat | 设置页新增侧边栏宽度设置（外观设置块）：TextBox（Header=侧边栏宽度）+「更改」按钮；输入 150–600 内整数且 ≠ 当前已存值才激活按钮（`CanApplyNavigationPaneWidth`，Accent 样式常驻、IsEnabled 绑定）；点击写 `config.NavigationPaneWidth`（UpdateAndSaveAsync）+ `MainWindow.ApplyNavigationPaneWidth` 运行时设 `OpenPaneLength` 立即生效（DP，无需重启；重启由 ApplyWindowStateFromConfig 复用同一值）；`LoadFromConfig` 初始化输入框。刻意**不加入 SaveAllToConfig 实时保存**——打字不落盘，仅点「更改」生效。静默生效无 Toast。构建验证通过 | main/ViewModels/SettingsViewModel.cs, main/MainWindow.xaml.cs, main/Pages/SettingsPage.xaml, main/Pages/SettingsPage.xaml.cs | 未提交 |
| 09:26 | ui | 侧边栏宽度设置交互微调：点击「更改」成功后清空输入框、焦点移出输入框（Focus 移回按钮）、按钮状态随 `CanApplyNavigationPaneWidth` 属性通知自动回灰（无需手动刷新） | main/Pages/SettingsPage.xaml.cs | 未提交 |